# Codex Rules

Repository operating preferences for this project:

- keep the app internal and operator-focused
- avoid pretending external provider APIs are live when they are not
- keep secrets server-side only
- avoid unrelated feature drift
- build one stable vertical slice at a time
- keep provider integrations behind an internal abstraction layer
- prefer maintainable structure over quick hacks
